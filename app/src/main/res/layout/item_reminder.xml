<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/white">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="14dp">

        <!-- Class Image / Avatar -->
        <ImageView
            android:id="@+id/ivClassImage"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:scaleType="centerCrop"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:src="@drawable/ic_launcher_background"
            android:background="@drawable/circle_image_background"
            android:clipToOutline="true"
            android:contentDescription="Class image" />

        <!-- Assignment Title -->
        <TextView
            android:id="@+id/tvAssignmentTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="10dp"
            android:text="Matematika Bab 5"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#1A1A2E"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/ivClassImage"
            app:layout_constraintEnd_toStartOf="@id/tvDeadlineBadge"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/tvClassInfo" />

        <!-- Class Info: Class Name + Submission Count -->
        <TextView
            android:id="@+id/tvClassInfo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="10dp"
            android:layout_marginTop="4dp"
            android:text="XI PPLG 3  •  30 dari 36 siswa sudah mengumpulkan"
            android:textSize="12sp"
            android:textColor="#6B7280"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/ivClassImage"
            app:layout_constraintEnd_toStartOf="@id/tvDeadlineBadge"
            app:layout_constraintTop_toBottomOf="@id/tvAssignmentTitle"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Deadline Badge (H-1, H-3, H-7) -->
        <TextView
            android:id="@+id/tvDeadlineBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="H-1"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:background="@drawable/bg_deadline_badge"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>